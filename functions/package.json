{
  "name": "procoach-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Firebase Functions for ProCoach Intervals.icu sync.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test",
    "deploy:gen1": "firebase deploy --only functions:getIntervalsData",
    "deploy:gen2": "FUNCTIONS_GEN=2 firebase deploy --only functions:getIntervalsData"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {}
}
